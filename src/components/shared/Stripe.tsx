import { MarqueeDemo } from "./Marquee";

const Stripe = () => {
  return (
    <div className="h-12 bg-green-secondary text-white flex">
      <MarqueeDemo />
    </div>
  );
};

export default Stripe;
